# Copy to .env.local and fill in. Used for local development only.

# SQLite database file (local dev)
DATABASE_URL="file:./dev.db"

# NextAuth – generate a secret: npx auth secret (or: openssl rand -base64 33)
# Only needed if you use auth in dev.
AUTH_SECRET="your-dev-secret-at-least-32-chars"

# Set to true to skip the survey gate and go straight to dashboard/roommates after login (for testing).
# NEXT_PUBLIC_SKIP_SURVEY_GATE=true
